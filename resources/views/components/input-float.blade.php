<div class="form-floating mt-3 mb-3">
    <input type="{{ $tipe }}" class="form-control {{ $attr }}" name="{{ $attr }}"
        id="{{ $attr }}" placeholder="{{ $placeholder }}">
    <label for="{{ $attr }}">{{ $text }}</label>
</div>
